from setuptools import setup

setup(
    name='qtproperties',
    version='0.1',
    description='QtWidgets for editing and displaying properties',
    author='Beat Reichenbach',
    packages=[
        'qtproperties'
        ],
    install_requires=[
        'PySide2',
        ]
)
